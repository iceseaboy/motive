---
name: ask-user-question
description: Ask the user a question via the Motive UI dialog.
metadata: { "defaultEnabled": true }
---

# AskUserQuestion

Use the `AskUserQuestion` MCP tool to ask the user for input.
Always include an `options` array (2–4 choices). The UI requires options.
